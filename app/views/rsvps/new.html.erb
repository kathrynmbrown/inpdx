<h1>RSVP to an event</h1>

  <%= form_for @rsvp do |f| %>
  <%= f.collection_select(:event_id, Event.all, :id, :name) %>
  <%= f.hidden_field :profile_id, :value => current_user.id  %>
  <%= f.submit "RSVP now", class: "button" %>
<% end %>
